import {StyleButton, ButtonText} from './Style';





export const Button = ({text}) => {
    return(
        <StyleButton>
            
            <ButtonText >
                {text}
            </ButtonText>
            
        </StyleButton>
    )
}